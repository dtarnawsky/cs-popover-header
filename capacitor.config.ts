import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-toolbar-scroll',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
